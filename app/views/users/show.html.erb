<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @user.name %>
</p>

<p>
  <strong>Email:</strong>
  <%= @user.email %>
</p>

Showing all of <%= @user.name %>'s Boards:
	<ul>
		<% @user.boards.each do |board| %>
		<li><%= board.name %></li>
		<% end %>
	</ul>

<%= link_to 'Edit', edit_user_path(@user) %> |
<%= link_to 'Back', users_path %>
